# ResicoQCM Backend Environment Variables
# Copy this file to .env and fill in your actual values

# Database Connection (Supabase PostgreSQL)
# Get this from: Supabase Dashboard → Settings → Database → Connection String (URI)
SUPABASE_DB_URL=postgresql://postgres:yacine2005a.@db.heehqgaskgzqxamknbpp.supabase.co:5432/postgres

# For Render deployment, use this variable name instead:
# DATABASE_URL=postgresql://postgres:yacine2005a.@db.heehqgaskgzqxamknbpp.supabase.co:5432/postgres

# JWT Secret for authentication tokens
# Generate a secure random string: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-at-least-32-characters

# Server Port (optional, defaults to 8001)
PORT=8001

# Node Environment
NODE_ENV=development

# Frontend URL for CORS (production)
FRONTEND_URL=https://your-vercel-app.vercel.app

# ============================================
# DEPLOYMENT INSTRUCTIONS
# ============================================
# 
# For LOCAL development:
# 1. Copy this file: cp .env.example .env
# 2. The values above are already set for your Supabase database
# 3. Generate a new JWT_SECRET (see command above)
# 4. Run: npm start
#
# For RENDER deployment:
# 1. Go to: https://dashboard.render.com/
# 2. Select your service: qcmbackend-i16b
# 3. Go to Environment tab
# 4. Add these environment variables:
#    - DATABASE_URL: postgresql://postgres:yacine2005a.@db.heehqgaskgzqxamknbpp.supabase.co:5432/postgres
#    - JWT_SECRET: (generate a new secure random string)
#    - NODE_ENV: production
#    - FRONTEND_URL: https://your-vercel-app.vercel.app
# 5. Save and wait for automatic redeploy
#
# SECURITY NOTE:
# - Never commit .env file to git (it's in .gitignore)
# - Use strong random strings for JWT_SECRET in production
# - Keep your database password secure
